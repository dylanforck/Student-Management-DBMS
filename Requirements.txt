# ------------------------------------------------------------------
# Core web framework
# ------------------------------------------------------------------
Flask>=2.3.0,<3.0
#   Flask is our microframework for routing, templating, sessions, etc.
#   We pin to 2.x to avoid any breaking changes in a future 3.0 release.

# ------------------------------------------------------------------
# MySQL driver
# ------------------------------------------------------------------
mysql-connector-python>=8.0.0,<9.0
#   Official Oracle MySQL driver for Python.
#   Enables our app’s database layer (open_connection, execute_query).

# ------------------------------------------------------------------
# Environment loading (optional, but recommended)
# ------------------------------------------------------------------
python-dotenv>=1.0.0
#   Lets you store env‑vars in a local .env file during development.
#   If you add a .env containing MYSQL_URL, FLASK_SECRET_KEY, etc.,
#   python-dotenv will load them automatically on app startup.

# ------------------------------------------------------------------
# Production WSGI server (for deployment)
# ------------------------------------------------------------------
gunicorn>=20.0.0,<21.0
#   A robust, pre‑fork worker model WSGI server.
#   Recommended instead of Flask’s built‑in server for production.

# ------------------------------------------------------------------
# (Optional) Code reloading in development
# ------------------------------------------------------------------
watchdog>=3.0.0
#   Provides file‑system event monitoring.
#   Can be used with Flask’s debug mode to auto‑reload on code changes.
